-- ECS
--~ local ECS = require 'lib.ECS'
--~ local Component, System, World = ECS.Component, ECS.System, ECS.World
--~ local ecs = { component = Component, system = System, world = World }

-- tiny-ecs
local ecs = require 'lib.tiny'

return ecs
